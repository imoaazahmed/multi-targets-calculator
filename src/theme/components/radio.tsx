import { extendVariants, Radio as ORadio, RadioGroup as ORadioGroup } from '@nextui-org/react';

export const Radio = extendVariants(ORadio, {
  defaultVariants: {},
});

export const RadioGroup = extendVariants(ORadioGroup, {
  defaultVariants: {},
});
