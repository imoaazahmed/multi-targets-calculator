import {
  extendVariants,
  Modal as OModal,
  ModalBody as OModalBody,
  ModalContent as OModalContent,
  ModalFooter as OModalFooter,
  ModalHeader as OModalHeader,
} from '@nextui-org/react';

export const Modal = extendVariants(OModal, {});

export const ModalContent = extendVariants(OModalContent, {});

export const ModalHeader = extendVariants(OModalHeader, {});

export const ModalBody = extendVariants(OModalBody, {});

export const ModalFooter = extendVariants(OModalFooter, {});
