import { extendVariants, Listbox as OListbox, ListboxItem as OListboxItem } from '@nextui-org/react';

export const Listbox = extendVariants(OListbox, {});

export const ListboxItem = extendVariants(OListboxItem, {});
